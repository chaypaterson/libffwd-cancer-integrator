TODO:

makefile for test projects two-hit.cpp and two-hit-characteristic.cpp

two-hit-characteristic.cpp compiles with
    g++ $file -o characteristic

two-hit.cpp needs
    g++ $file -lgsl -pthread -o two-hit

better naming convention would also be good
